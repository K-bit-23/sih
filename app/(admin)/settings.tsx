import { Text, View } from 'react-native';

export default function AdminSettingsPage() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 24 }}>Admin Settings Page</Text>
    </View>
  );
}
